@model IEnumerable<Menu.Client.Models.ItemViewModel>

@{
    ViewBag.Title = "Позиции меню";
}

<br />
<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">
            <h3>
                Список позиций меню
            </h3>
        </h3>
    </div>
    <div class="panel-body">
        <p>
            @Html.ActionLink("Добавить позицию", "Create", null, new {@class = "btn btn-success btn-large"})
            @Html.ActionLink("Перейти к категориям", "Index", "Home", null, new {@class = "btn btn-success btn-large"})
        </p>

        <table class="table">
            <tr>
                <th>
                    Наименование
                </th>
                <th>
                    Количество
                </th>
                <th>
                    Стоимость
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Amount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Price) руб.
                    </td>
                    <td>
                        @Html.ActionLink(" ", "Edit", new {id = item.Id}, new {@class = "btn btn-primary glyphicon glyphicon-edit"}) |
                        @Html.ActionLink(" ", "Delete", new {id = item.Id}, new {@class = "btn btn-danger glyphicon glyphicon-remove"})
                    </td>
                </tr>
            }

        </table>

    </div>
</div>

